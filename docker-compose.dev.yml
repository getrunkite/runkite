# Zero-dependency development stack, for local DX.
# Control plane + Python runner only — no Postgres/Redis.
# Uses embedded SQLite + in-memory transport.
#
# Usage:
#   docker compose -f docker-compose.dev.yml up -d --build
#   docker compose -f docker-compose.dev.yml down -v
services:
  runkite:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2026:2026"
      - "50051:50051"
    environment:
      HTTP_PORT: "2026"
      GRPC_PORT: "50051"
      LANGGRAPH_CONFIG: /app/examples/all_agents/langgraph.json
      DATABASE_PATH: /tmp/runkite.db
      # This is the intentional zero-dependency local profile (SQLite,
      # in-process transport, no auth) -- checkProductionAdmission
      # (cmd/serve.go) would otherwise refuse to start `serve` with
      # that posture. Never set this for an internet-facing deployment.
      RUNKITE_ALLOW_INSECURE_SERVE: "1"
    volumes:
      - ./examples:/app/examples:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:2026/readyz"]
      interval: 5s
      timeout: 3s
      retries: 5

  runner:
    build:
      context: .
      dockerfile: Dockerfile.runner
    environment:
      RUNKITE_GRPC_URL: runkite:50051
      RUNKITE_HTTP_URL: http://runkite:2026
    volumes:
      - ./python:/app/python:ro
      - ./examples:/app/examples:ro
    depends_on:
      runkite:
        condition: service_healthy
